import { IItemOption } from "./../../models/user";

export const AddCollectionService = async (
  id: string,
  title: string,
  description: string,
  image: string,
  topic: string,
  itemsOptions: IItemOption[]
) => {
  return {};
};
